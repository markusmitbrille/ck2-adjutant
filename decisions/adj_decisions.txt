decisions = {
    adj_manage_titles_limit = {
        is_high_prio = yes
        only_playable = yes
        only_rulers = yes
        ai = no
        
        potential = {
            is_ruler = yes
            is_nomadic = no
            higher_real_tier_than = COUNT
            over_max_demesne_size > 0
            NOT = { has_character_flag = adj_character_flag_managing_titles }
        }

        allow = {
            custom_tooltip = { text = adj_decision_allow_must_have_bureaucrat_tooltip }
            hidden_tooltip = {
                # has bureaucrat
                any_courtier_or_vassal = {
                    has_minor_title = adj_title_bureaucrat
                    liege = { character = PREVPREV }
    
                    always = yes
                }
            }

            # has vast demesne
            over_max_demesne_size > 0
        }

        effect = {
            character_event = { id = ADJ.10 tooltip = adj_evt_tooltip_10 }
        }
    }
    adj_manage_titles_new = {
        is_high_prio = yes
        only_playable = yes
        only_rulers = yes
        ai = no
        
        potential = {
            is_ruler = yes
            is_nomadic = no
            higher_real_tier_than = COUNT
            NOT = { has_character_flag = adj_character_flag_managing_titles }
            any_demesne_title = {
                has_holding_modifier = adj_event_modifier_newly_acquired
                always = yes
            }
        }

        allow = {
            custom_tooltip = { text = adj_decision_allow_must_have_bureaucrat_tooltip }
            custom_tooltip = { text = adj_decision_allow_must_have_new_titles_tooltip }
            hidden_tooltip = {
                # has bureaucrat
                any_courtier_or_vassal = {
                    has_minor_title = adj_title_bureaucrat
                    liege = { character = PREVPREV }
                }
    
                # has new titles
                any_demesne_title = {
                    has_holding_modifier = adj_event_modifier_newly_acquired
                    always = yes
                }
            }
        }

        effect = {
            character_event = { id = ADJ.20 tooltip = adj_evt_tooltip_20 }
        }
    }
    adj_manage_vassals = {
        is_high_prio = yes
        only_playable = yes
        only_rulers = yes
        ai = no
        
        potential = {
            is_ruler = yes
            is_nomadic = no
            higher_real_tier_than = COUNT
            NOT = { has_character_flag = adj_character_flag_managing_titles }
            over_vassal_limit > 0
        }

        allow = {
            custom_tooltip = { text = adj_decision_allow_must_have_bureaucrat_tooltip }
            hidden_tooltip = {
                # has bureaucrat
                any_courtier_or_vassal = {
                    has_minor_title = adj_title_bureaucrat
                    liege = { character = PREVPREV }
                }
            }
            
            over_vassal_limit > 0
        }

        effect = {
            character_event = { id = ADJ.30 tooltip = adj_evt_tooltip_30 }
        }
    }
}